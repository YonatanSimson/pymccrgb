[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pymccrgb"
version = "0.1.7"
description = "A Python package for point cloud classification using color and curvature"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Robert Sare", email = "rmsare@stanford.edu" },
    { name = "George Hilley" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
    "cython",
    "numpy",
    "matplotlib",
    "pdal",
    "scipy",
    "scikit-image",
    "scikit-learn",
    "pymcc_lidar",
]

[project.optional-dependencies]
rasterio = ["rasterio"]  # fallback for write_dem when PDAL/GDAL is unavailable or fails
vertical = ["pyproj"]    # for ellipsoidal -> vertical CRS height conversion in write_dem

[project.urls]
Homepage = "https://github.com/rmsare/pymccrgb"

[tool.setuptools.packages.find]
where = ["."]
